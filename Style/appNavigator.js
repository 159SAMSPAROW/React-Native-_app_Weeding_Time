import { StyleSheet } from 'react-native';

const appNavigator = StyleSheet.create({

  textStyle: {
    alignSelf: 'flexStart',
  },

  containerStyle: {
    height: 25,
  },
});

export default appNavigator;