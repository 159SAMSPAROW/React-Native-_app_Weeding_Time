import { StyleSheet } from "react-native";

export const weedingMealPage = StyleSheet.create({
  title: {
    position: "absolute",
    top: 20,
    left: 120,
    zIndex: 1,
    color: "#6750A4",
  },
});
